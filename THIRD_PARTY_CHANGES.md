# Third-party changes

No third-party source files have been modified. `app/hoyolab/hoyolab_auto_adapter.js` is new product code whose supported-game endpoint constants were derived from the unmodified `hoyolab-auto` snapshot; it replaces its global-runtime model with the desktop application's credential boundary and normalized result model.
